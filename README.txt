String Search in Java using the Net Beans IDE.

Program prompts user to enter a file path to access a file. Users are
requested to input a ‘string’ that they want to search for in the file.
The program searches through the file to locate the requested string
and outputs the occurrence of the string.